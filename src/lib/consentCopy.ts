// Client-safe consent copy, shared between the onboarding UI and the
// server-side consent ledger (lib/consent.ts) so the recorded
// consentTextVersion always matches what the user actually saw.
export const CONSENT_TEXT_VERSION = "v1";
export const CONSENT_COPY =
  "I want to receive my personalized music radar via email.";
